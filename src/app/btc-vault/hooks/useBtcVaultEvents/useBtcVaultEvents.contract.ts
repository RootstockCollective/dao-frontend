/**
 * Placeholder for contract-based event subscription.
 * When using on-chain contracts, wagmi handles refetching automatically.
 */
export function useBtcVaultEventsContract() {
  // No-op: wagmi handles refetching for contract-based hooks
}
