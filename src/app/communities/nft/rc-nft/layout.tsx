import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Rootcamp NFT',
}

export default function RcNftLayout({ children }: { children: React.ReactNode }) {
  return children
}
