import { FC } from 'react'
import { cn } from '@/lib/utils'

type BackingCellProps = {
  className?: string
}

export const BackingCell: FC<BackingCellProps> = ({ className }) => {
  return <div />
}
