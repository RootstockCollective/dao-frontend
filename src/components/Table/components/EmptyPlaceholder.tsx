import { Span } from '@/components/Typography'

/**
 * Common empty placeholder component for table cells
 * Used when a table cell has no data to display
 */
export const EmptyPlaceholder = () => {
  return <Span>-</Span>
}
